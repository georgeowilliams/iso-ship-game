/**
 * @typedef {Object} InputAdapter
 * @property {(onVote: (vote: { userId: string, choice: string, weight?: number }) => void) => void} start
 * @property {() => void} stop
 */

export const INPUT_ADAPTER_DOC = true;
